# Backend Supabase (opsional) — sinkronisasi antar-perangkat.
# Salin file ini jadi `.env` lalu isi dari dashboard Supabase: Project Settings -> API.
# Tanpa ini, SUUCHAT tetap jalan 100% (mode lokal per-perangkat).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
